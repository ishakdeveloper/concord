app = "concord-ws"
primary_region = "lax"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "4001"
  MIX_ENV = "prod"

[http_service]
  internal_port = 4001
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    grace_period = "60s"
    interval = "30s"
    method = "GET"
    timeout = "10s"
    path = "/health"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024

[deploy]
  release_command = "mix deps.get && mix deps.compile" 